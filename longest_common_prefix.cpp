#include <map>
#include <iostream>
#include <vector>
#include <string>
#include <stack>

using namespace std;

string longestCommonPrefix(vector<string>& strs) {

    int len = strs.size();
    if(len == 0) return  "";

    string a = "";

    for(int i = 0; i < ; i++){

    }

    return a;
}

int main() {
    vector<string> strs = {"flower","flow","flight"};

    return 0;
}